# 새 프로젝트 시작 가이드

현재 프로젝트와 동일한 작업환경으로 새 프로젝트를 시작하는 방법입니다.

---

## 1단계: 프로젝트 폴더 생성 및 초기화

```bash
# 새 프로젝트 폴더 생성
mkdir 새프로젝트명
cd 새프로젝트명

# Git 초기화 (선택사항)
git init
```

---

## 2단계: package.json 생성

프로젝트 루트에 `package.json` 파일 생성:

```json
{
  "name": "새프로젝트명",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build --turbopack",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "15.5.2",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "autoprefixer": "^10.4.21",
    "eslint": "^9",
    "eslint-config-next": "15.5.2",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.12",
    "typescript": "^5"
  }
}
```

**필요한 추가 패키지가 있다면:**
- Firebase: `firebase`, `firebase-admin`
- PDF: `jspdf`, `html2pdf.js`, `html2canvas`
- Excel: `xlsx`
- 기타 필요한 패키지 추가

---

## 3단계: TypeScript 설정 (tsconfig.json)

프로젝트 루트에 `tsconfig.json` 파일 생성:

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

---

## 4단계: Next.js 설정 (next.config.ts)

프로젝트 루트에 `next.config.ts` 파일 생성:

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
```

---

## 5단계: PostCSS 설정 (postcss.config.mjs)

프로젝트 루트에 `postcss.config.mjs` 파일 생성:

```javascript
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
```

---

## 6단계: ESLint 설정 (eslint.config.mjs)

프로젝트 루트에 `eslint.config.mjs` 파일 생성:

```javascript
import { dirname } from "path";
import { fileURLToPath } from "url";
import { FlatCompat } from "@eslint/eslintrc";

const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);

const compat = new FlatCompat({
  baseDirectory: __dirname,
});

const eslintConfig = [
  ...compat.extends("next/core-web-vitals", "next/typescript"),
  {
    ignores: [
      "node_modules/**",
      ".next/**",
      "out/**",
      "build/**",
      "next-env.d.ts",
    ],
  },
];

export default eslintConfig;
```

---

## 7단계: .gitignore 생성

프로젝트 루트에 `.gitignore` 파일 생성:

```
# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js
.next/
out/
build/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory
coverage/
.nyc_output

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# Cache
.cache
.parcel-cache

# next.js build output
.next

# nuxt.js build output
.nuxt

# vuepress build output
.vuepress/dist

# Serverless directories
.serverless

# FuseBox cache
.fusebox/

# DynamoDB Local files
.dynamodb/

# TernJS port file
.tern-port
```

---

## 8단계: 기본 폴더 구조 생성

```bash
# src 폴더 구조 생성
mkdir -p src/app
mkdir -p src/components
mkdir -p src/lib
mkdir -p src/utils
mkdir -p public
```

또는 수동으로 폴더 생성:
- `src/app/`
- `src/components/`
- `src/lib/`
- `src/utils/`
- `public/`

---

## 9단계: 기본 파일 생성

### src/app/layout.tsx
```typescript
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "새 프로젝트",
  description: "새 프로젝트 설명",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ko">
      <body>{children}</body>
    </html>
  );
}
```

### src/app/page.tsx
```typescript
export default function Home() {
  return (
    <main>
      <h1>새 프로젝트</h1>
    </main>
  );
}
```

### src/app/globals.css
```css
@import "tailwindcss";
```

---

## 10단계: 의존성 설치 및 실행

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

브라우저에서 `http://localhost:3000` 접속하여 확인

---

## 11단계: Git 저장소 연결 (선택사항)

```bash
# 원격 저장소 추가
git remote add origin <저장소_URL>

# 첫 커밋
git add .
git commit -m "Initial commit"
git push -u origin main
```

---

## 📝 체크리스트

- [ ] 프로젝트 폴더 생성
- [ ] package.json 생성
- [ ] tsconfig.json 생성
- [ ] next.config.ts 생성
- [ ] postcss.config.mjs 생성
- [ ] eslint.config.mjs 생성
- [ ] .gitignore 생성
- [ ] 기본 폴더 구조 생성
- [ ] 기본 파일 생성 (layout.tsx, page.tsx, globals.css)
- [ ] npm install 실행
- [ ] npm run dev 실행 확인
- [ ] Git 초기화 및 커밋 (선택)

---

## 🎯 다음 단계

1. Firebase 설정이 필요하면 `src/lib/firebase.ts` 생성
2. 필요한 컴포넌트 생성
3. 라우팅 설정
4. 스타일링 작업

---

**작성일**: 2025년


